package adaptorDsignPattern.JavaPracticleExample;

/**
 * Problem Statement: Earlier we have Enumeration interface that discloses two methods - hasMoreElements(), nextElement()
 * but now a new interface introduced - Iterator, that discloses three methods - hasNext(), next(), remove()
 *
 * Let's say some codebases uses Enumeration but they want to change to Iterator for more functionality - Adaptor Design Pattern.
 */
public class TestDrive {
    public static void main(String[] args) {
        
    }
}
