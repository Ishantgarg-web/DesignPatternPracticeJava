package iteratorDesignPattern;

/**
 * Problem statement:
 * Let's say you have a collection of objects, and you want to provide a way
 * to access those objects sequentially without exposing the
 * underlying representation of the collection.
 * This is where the Iterator Design Pattern comes in.
 *
 * Example: You have a collection of books in a library, and you want to provide
 * a way to access those books one by one without exposing the underlying data
 * structure of the collection.
 */
public class Main {
}
