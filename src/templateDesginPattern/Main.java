package templateDesginPattern;

/**
 * Problem statement: You have two classes - Coffee and Tea.
 * and Recepie for making coffee and Tea is in approach 1.
 *
 * Its, basically removes duplication from code and write a code in reusable form.
 *
 * Template Design pattern: defines the skelton of an algorithm in a method, deferring
 * some steps to subclasses. Template method lets subclasses redefine certain steps
 * of an algorithm without changing the algorithm's structure.
 *
 * Meaning: Define methods as abstract in superclass - which can override in subclasses.
 */
public class Main {
}
